import { fetch } from './fetch'
import { serve } from './serve'

export { LightpandaFetchOptions } from './fetch'
export { LightpandaServeOptions } from './serve'

export const lightpanda = {
  fetch,
  serve,
}
