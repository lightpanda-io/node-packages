---
"@lightpanda/browser": minor
---

- Feat: high-level `Browser` / `Session` API driving the browser tools (the MCP tool set: `goto`, `markdown`, `extract`, `click`, `fill`, ...) with typed methods
- Feat: `CDPServer` and `BiDiServer` to run `lightpanda serve` for Puppeteer, Playwright and WebDriver BiDi clients
- Feat: `dump` and `runScript` for one-shot page dumps and saved script replays, with typed browser options
- `lightpanda.fetch` and `lightpanda.serve` are unchanged
